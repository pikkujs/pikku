---
"@pikku/backblaze": patch
---

Compute SHA1 content hash for server-side uploads instead of using do_not_verify. Implement time-limited signed URLs via B2's download authorization API for signContentKey and signURL.
