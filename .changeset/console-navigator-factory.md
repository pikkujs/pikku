---
'@pikku/console': patch
---

Add ConsoleNavigatorContext (OSSConsoleNavigator reads ?id= from the query param; hosts override via ConsoleNavigatorCtx) and export the pikku factory for external host adapters.
