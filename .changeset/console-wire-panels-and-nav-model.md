---
'@pikku/console': patch
---

Channels, CLI and gateways get list panels like the other five wire kinds, so every kind is a panel a host can mount on its own. The nav is now one model in `src/nav/sections.ts` that the dock, mobile sheet and spotlight all read.
