---
"@pikku/console": patch
---

Show function version sub-rows with version history, live queue depths and a Failed column in the Queues grid, and last run + status with run history in the Schedulers grid.
