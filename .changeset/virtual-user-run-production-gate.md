---
'@pikku/console': patch
---

`VirtualUsersWorkspace` takes a `production` flag. When set, Run now is disabled for every persona whose disposition is not accountable, with a line saying why, instead of offering a run the server can only refuse.
