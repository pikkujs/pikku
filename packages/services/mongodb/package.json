{
  "name": "@pikku/mongodb",
  "version": "0.12.18",
  "description": "MongoDB services for Pikku — workflow state, agent runs, secrets and channel stores",
  "author": "yasser.fadl@gmail.com",
  "license": "MIT",
  "module": "dist/src/index.js",
  "main": "dist/src/index.js",
  "type": "module",
  "scripts": {
    "tsc": "tsc",
    "ncu": "npx npm-check-updates",
    "build": "tsc -b",
    "test": "bash run-tests.sh",
    "test:watch": "bash run-tests.sh --watch",
    "test:coverage": "bash run-tests.sh --coverage",
    "release": "npm run build && npm test",
    "prepublishOnly": "yarn build"
  },
  "peerDependencies": {
    "@pikku/core": "^0.12.84"
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  },
  "devDependencies": {
    "mongodb": "^7.5.0",
    "mongodb-memory-server": "^11.2.0",
    "typescript": "^6.0.3"
  },
  "exports": {
    ".": "./dist/src/index.js"
  }
}
