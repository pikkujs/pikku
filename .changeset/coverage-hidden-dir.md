---
"@pikku/cli": patch
---

Output coverage artifacts to `.coverage/` instead of `coverage/` so the directory is hidden by default and consistent with the `.gitignore` convention for generated outputs.
