---
'@pikku/skills': patch
---

The pikku-guide skill's capture-step example is now compiled from `examples/online-shop`, and uses named schemas. The inline `z.object` it showed before is rejected by pikku (PKU489).
