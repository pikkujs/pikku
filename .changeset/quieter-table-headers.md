---
'@pikku/console': patch
---

Quieter list headers. The column labels were uppercase, letter-spaced, semibold and inheriting the monospace stack, which read as a shouty band above the rows; they are now sentence-case sans at normal tracking in a slightly taller row.
