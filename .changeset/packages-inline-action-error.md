---
'@pikku/console': patch
---

Show install/import failures inline in the addon detail drawer (a red Alert above the CTA) instead of a toast notification. Success is already conveyed inline by the card/drawer flipping to the Added/Imported badge, so no separate success message is needed.
