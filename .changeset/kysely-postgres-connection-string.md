---
"@pikku/kysely-postgres": patch
---

Support connection string URLs in PikkuKysely constructor. You can now pass a `DATABASE_URL` string directly instead of only config objects or existing Sql instances.
