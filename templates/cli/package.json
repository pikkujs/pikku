{
  "name": "@pikku/templates-cli",
  "type": "module",
  "private": true,
  "description": "Pikku CLI template showcasing both local and remote (channel-based) CLI approaches",
  "scripts": {
    "pikku": "pikku all",
    "build": "tsc -b",
    "tsc": "tsc",
    "cli": "tsx src/cli-local.gen.ts",
    "cli:local": "tsx src/cli-local.gen.ts",
    "cli:remote": "tsx src/cli-remote.gen.ts",
    "server": "tsx src/server.ts",
    "dev:server": "tsx watch src/server.ts",
    "ncu": "npx npm-check-updates",
    "ncu:pikku": "npx npm-check-updates -f '/@pikku/.*/'"
  },
  "dependencies": {
    "@pikku/core": "^0.10.0",
    "@pikku/uws": "^0.10.0",
    "@pikku/websocket": "^0.10.0"
  },
  "devDependencies": {
    "@pikku/cli": "^0.10.0",
    "@types/node": "^24",
    "tsx": "^4.20.6",
    "typescript": "^5.9"
  }
}
