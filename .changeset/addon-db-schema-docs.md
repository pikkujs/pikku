---
'@pikku/skills': patch
---

Document how an addon ships database tables: `pikku db export`, the artifact the
consumer resolves through the package name, and the silent failure when it is
neither exported nor packed.
