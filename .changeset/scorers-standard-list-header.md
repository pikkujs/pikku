---
'@pikku/console': patch
---

The Scorers screen wears the same header as every other list screen: title, description and search in the ShellHeader band rather than a bare input strip inside the table, and its column labels come from the messages that were already written for them.
