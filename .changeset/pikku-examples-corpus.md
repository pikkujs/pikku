---
'@pikku/cli': patch
---

`pikku examples` — 39 worked examples of the things that are easy to get wrong and hard to discover, read with `examples show` or written into the project with `examples add --entity <yours>` already renamed onto your own domain.
