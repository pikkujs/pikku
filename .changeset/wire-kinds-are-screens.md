---
'@pikku/console': patch
---

Each wire kind is its own screen: the APIs and Jobs tabbed surfaces are replaced by /wires/{http,channel,mcp,cli,gateway} and /async/{scheduler,queue,trigger}, each exported as its own page. The dock's Build group lists them under Source, Wiring, Async, Where it runs and Shipping.
