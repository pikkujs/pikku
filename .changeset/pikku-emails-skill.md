---
'@pikku/cli': patch
---

docs(skills): add the `pikku-emails` skill documenting file-based email templates — directory layout, templating syntax, per-template typed variables, `pikku emails generate`, and rendering/sending through an EmailService.
