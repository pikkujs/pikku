---
'@pikku/console': patch
---

Keep the selected scenario run in the URL (`?view=runs&run=<runId>`), so a run is a link you can send and a host console can point straight at the run a build produced.
