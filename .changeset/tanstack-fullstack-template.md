---
'create-pikku': patch
---

Add a `tanstack` fullstack template: a TanStack Start SPA served by `pikku serve` from the same origin as the API, with a passphrase unlock screen driving the data-lock routes.
