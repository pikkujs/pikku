---
'@pikku/skills': patch
---

`pikku-scenario` and `pikku-concepts` load lean: their long sections move into references (`steps`, `personas`, `browser`, `coverage`; `bootstrap`, `language`) that the skill body links to, and the long references in `pikku-auth`, `pikku-build`, `pikku-concepts` and `pikku-react` open with a table of contents.
