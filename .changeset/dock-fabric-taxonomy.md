---
'@pikku/console': patch
---

The dock's group menus use Fabric's nav taxonomy — People, AI, Testing, Operate, Build, with the same titled bands inside them (Configuration, Connections, Wiring, Where it runs, and the rest), so the same shelf is called the same thing in both consoles.
