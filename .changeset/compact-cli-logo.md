---
'@pikku/cli': patch
---

Replace the multi-line ASCII-art logo with a compact one-line wordmark (`◇◆ pikku ::`). The tall logo was getting cropped in short/narrow AI-agent terminal panes; the one-line mark can't be clipped vertically and renders its colours directly instead of through the info path.
