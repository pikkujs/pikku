---
'@pikku/console': patch
---

Hovering a scenario step now highlights the thing it will act on — the actor, or the step's sentence — rather than the whole row. The row holds two destinations (the actor opens their persona, everything else opens the step), so a row-wide highlight promised one action for both.
