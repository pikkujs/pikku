---
'@pikku/cli': patch
---

Describe how each export is called in the surface doc: computed signatures, the keys of the options object it takes with the JSDoc that declares them, `@example` blocks, and the HTTP status an error class maps to. Each leaf now also names the skill that teaches it, or declares that none does.
